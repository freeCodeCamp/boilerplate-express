let express = require('express');
let app = express();
let PORT = 3003;

app.listen(console.log(`Server started running on ${PORT}`) || process.env.PORT);




































 module.exports = app;
